# Earnings Call Analyzer -- pinned dependencies (Python 3.11)
#
# Windows notes:
#   * chromadb pulls in onnxruntime for the default embedding function. The
#     prebuilt wheel usually installs cleanly, but may require the Microsoft
#     Visual C++ Redistributable at runtime (not the full Build Tools).
#   * Do NOT add sentence-transformers -- its tokenizers pin conflicts with
#     chromadb's. Stick to chromadb's onnx default embedding function.
#   * First run downloads the MiniLM model (~90MB).

streamlit==1.40.2
langgraph==0.2.60
langchain-core==0.3.86
chromadb==0.5.23
anthropic>=0.69.0
requests==2.32.3
python-dotenv==1.0.1
pydantic==2.10.4
beautifulsoup4==4.12.3
lxml==5.3.0
pytest==8.3.4
