line-length = 100
target-version = "py311"

[lint]
select = ["E", "F", "I"]   # pycodestyle errors, pyflakes, import sorting
ignore = ["E501"]          # line length handled by the formatter, not enforced
