line-length = 100
target-version = "py311"

[lint]
select = ["E", "F", "I"]   # pycodestyle errors, pyflakes, import sorting
ignore = ["E501", "E402"]  # E501: line length; E402: deliberate late import (secrets bridge)
